#include "CppUTest/TestHarness.h"
#include "DetectSettings.h"

/* 
This blob is for detecting setings

This can be done less frequently than polling the keys.

How to transfer the settings to main program?!?
*/



TEST_GROUP(DetectSettings)
{
    void setup()
    {
    }

    void teardown()
    {
    }
};

TEST(DetectSettings, something)
{
    //DOUBLES_EQUAL(10, GetdBValue(600,10,10),1);
}
